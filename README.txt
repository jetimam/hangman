# A simple Hang Man game.
---------------------------------------------------
In order to run the game you need to have Maven installed. You can do that by running the command;
`sudo apt install maven`

You can run the game by navigating to the root folder and running the command;
`mvn exec:java -Dexec.mainClass="proj.hang.Main"`
